import "package:flutter/material.dart";

class AppColors {
  static const Color white = Color(0xFFFFFFFF);
  static const Color primaryBlue = Color(0xFF416FDF); // The vibrant blue
  static const Color lightGreyBackground = Color(
    0xFFF7F8F9,
  ); // For text field background
  static const Color borderGrey = Color(0xFFE8ECF4); // For text field borders
  static const Color darkGreyText = Color(
    0xFF8391A1,
  ); // Placeholder/secondary text
  static const Color darkBlueText = Color(
    0xFF1F2937,
  ); // For "Get Started" and "Sign up with"
  static const Color black = Color(0xFF000000); // For some text if needed
  static const Color purpleAccent = Color(0xFF5E3AD8); // For abstract shapes
  static const Color lightPurpleAccent = Color(
    0xFFB1B6FF,
  ); // For abstract shapes
  static const Color deepPurple = Color(
    0xFF5A44E3,
  ); // A slightly darker purple for elements
  static const Color secondaryBlue = Color(
    0xFF3366FF,
  ); // For "Personal data" link
}
